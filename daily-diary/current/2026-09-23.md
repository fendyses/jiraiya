# Daily Diary - September 23, 2026
*Session documentation and development record*

---

## 2026-09-23 (Morning - 09:51) - MyAlumniCard: Himpunan Mesra Alumni 2026 Banner on Home

### Session Summary
- Type: Work
- Repo: MyAlumniCard (`/Applications/Sites/myalumni-angular`) — UiTM
- Focus: Put the Himpunan Mesra Alumni UiTM 2026 poster on the home page, with a registration button over it.
- Outcome: Banner + "Register Now" button added to `src/app/home/home.component.html`; change is staged but **not committed**, and not yet built or viewed in a browser.

### Main Topics Discussed

1. **Placement.** Fendy marked the spot from a phone screenshot: under the benefits carousel dots, above the convocation card. Inserted as the first `col-12` in the row after the `<splide>`, outside the `convocation?.open && isGraduated` block so every user sees it. Markup follows the commented-out Madani UiTM Run banner already in the file (rounded 8px wrapper, full-width `img`). CDN URL checked: HTTP 200, 156 KB JPEG, 1280×1280.
2. **Button on the picture.** Anchor with `target="_blank" rel="noopener"` to `https://alumniuitm.my/program/pendaftaran_penyertaan_himpunan/` (same effect as `goToLink()`, which is just `window.open(url, "_blank")`). First draft pinned it 16px from the bottom — viewing the poster showed that would sit on the Anjuran/Kerjasama/Rakan Strategik logos, so it moved to `top: 81%` centred, the empty band between "PENYERTAAN" and "ANJURAN".
3. **Visibility.** White-on-purple wasn't prominent enough for Fendy; changed to amber `#ffc107` with near-black bold text and a 2px white border — the complementary colour to the poster's purple.

### Key Insights & Learning
- Look at the actual image before overlaying anything on it — a "bottom" button would have covered the organiser logos.
- Percentage positioning on a square poster keeps the button in the same band at any card width.

### Collaboration Highlights
- Fast, visual loop: screenshot with an arrow → placement; one-line asks for the button and colour.

### Looking Forward
- Build / view on a phone-width screen to confirm the button sits in the gap.
- Label is English "Register Now" to match REDEEM NOW / VISIT NOW; offer stands to switch to "Daftar Sekarang".
- Commit + deploy when Fendy is happy.
- Still paused from 17 Sep: semakV2 null `alumni_id` investigation (needs `gcloud auth login`).

---

## 2026-09-23 (Morning - 10:51) - Nilam: Editable Partner Contact Details in Manage Partners Modal

### Session Summary
- Type: Work
- Repo: Nilam (`/Applications/Sites/nilam`) — UiTM
- Focus: On Manage Legal Documents (applications index), let admins edit Person In Charge, Email, Office Phone and Mobile Phone after picking a partner in the Edit Partner modal.
- Outcome: The four inputs in `resources/views/applications/index.blade.php` were `readonly`; removed that and relabelled the placeholder to "Auto-filled from partner data, editable". Fendy committed it as `a576c10` on `development`. Not tested in a browser; not pushed.

### Main Topics Discussed
1. **Where the lock was.** The Select Partner dropdown (select2) calls `/api/partners/details/{id}` and fills the four fields; the fields themselves were `readonly`, so the auto-filled values couldn't be adjusted.
2. **No backend change needed.** `ApplicationController::addPartner()` already validates and saves `person_in_charge`, `email`, `office_phone_no`, `mobile_phone_number` from the request into the application–partner pivot. Edits stay per application; the partner master record is untouched (same concern as the 17 Sep SEDA→MGTC incident).

### Key Insights & Learning
- The pivot already carried per-application contact details, so the UI was the only thing preventing the override.

### Looking Forward
- Only covers **adding** a partner. Partners already under "Current Partners" still can't be edited in this modal (remove + re-add); offered to add an inline edit.
- Push `a576c10` + merge to `master` with the two 18 Sep commits, then deploy.
- `config/services.php` still deliberately unstaged; local `.env` status (prod vs `nilamsdev`) unverified this session.

---
