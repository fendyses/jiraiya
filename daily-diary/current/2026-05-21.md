# 📓 Daily Diary — 2026-05-21

> DevOps + dev day — agent symlinks distributed across all 14 repos, Firestore offline persistence added to mystudentvue.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
�� 13:09 GMT+8 · 🔧 DEVOPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**Global Agent Distribution — All Repos**

📌 Focus  : Distribute JIRAIYA agents to all 14 repos on this machine
✅ Result : All repos now have @jiraiya, @sescode, @sescheck, @sesinfra, @sesdocument

- Created `scripts/install-agents.sh` — symlinks agent files from jiraiya into any target repo
- Ran script across all 14 repos under `/Applications/ServBay/www/`
- Added `.github/agents/` to `.gitignore` in all 13 non-jiraiya repos (symlinks stay local, never committed)
- Updated `HOW-TO-USE.md` — added "Adding Agents to a New Repo" section

| File | Action |
|------|--------|
| `scripts/install-agents.sh` | ✅ Created |
| `HOW-TO-USE.md` | ✅ Updated |
| `<all 13 repos>/.gitignore` | ✅ Updated |

💡 **Decision:** Agents are symlinks (not copies) — updates to jiraiya source propagate everywhere automatically.

> *Logged by sesdocument — 2026-05-21*

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🕐 15:18 GMT+8 · ✨ FEATURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**Firestore Offline Persistence & Retry Logic — mystudentvue**

📌 Focus  : Firebase / Firestore reliability improvements
✅ Result : Committed and pushed to `development` branch

- Enabled **IndexedDB offline persistence** in `src/firebase.js` — serves cached data during dropped connections
- Updated `src/main.js` — persistence setup runs gracefully before app mount
- Added **retry logic** in `src/services/auth.service.js` — resilience against transient Firestore errors
- Minor alignment fix to `src/firebase/index.js`

| File | Action |
|------|--------|
| `src/firebase.js` | ✅ Added `enableIndexedDbPersistence` with error handling |
| `src/main.js` | ✅ Persistence init before Vue app mount |
| `src/services/auth.service.js` | ✅ Added retry logic |
| `src/firebase/index.js` | ✅ Minor alignment |
| `.gitignore` | ✅ Updated |

💡 **Note:** `enableIndexedDbPersistence` must be called immediately after `getFirestore` — before any reads/writes. Multi-tab caveat: only one active tab at a time (`failed-precondition` handled gracefully).

**Looking Forward**
- Monitor offline behaviour in staging
- Consider `enableMultiTabIndexedDbPersistence` if multi-tab support is needed

> *Logged by sesdocument — 2026-05-21 15:50 GMT+8*
