# Daily Diary - July 15, 2026
*Session documentation and development record*

---

## 2026-07-15 (Night - 11:23 PM GMT+8) - Installed Codex CLI, Wired It to the JIRAIYA Memory Core

### Session Summary
- Type: Work (Infrastructure/Tooling)
- Focus: Install OpenAI's Codex CLI and make it aware of the JIRAIYA memory system across all registered repos
- Outcome: Done — Codex installed globally, `AGENTS.md` created at both global (`~/.codex/`) and repo level, `main/repos.md` registry paths corrected for all 6 repos

### Main Topics Discussed
1. Fendy asked to install Codex and have it use the jiraiya repo as a reference for agents. Checked both npm (`@openai/codex`) and Homebrew cask — both offered the same version (0.144.4); Fendy chose npm global install.
2. Clarified intent: Fendy wants Codex, when run from any repo listed in the JIRAIYA repo registry (including jiraiya itself), to adopt the same memory-system structure that Claude Code follows via `CLAUDE.md`.
3. Discovered Codex has no skill/plugin auto-trigger mechanism like Claude Code's `plugins/ses-skills/` — it only reads `AGENTS.md` (global at `~/.codex/AGENTS.md`, merged with a repo-level `AGENTS.md`). Designed the setup around that constraint instead of assuming feature parity.
4. Found a path mismatch: `main/repos.md` listed all 6 registered repos under `/Applications/ServBay/www/...`, but every one of them actually lives under `/Applications/Sites/...` on disk. Verified each path individually before fixing the whole table (Jiraiya, Nilam, MyStudent, MyAlumniCard, Masmed2u, Mobiliti UG).
5. Created `~/.codex/AGENTS.md` (global) — checks whether cwd matches a repo in `main/repos.md`, and if so, defers to the JIRAIYA protocol rather than duplicating it.
6. Created `/Applications/Sites/jiraiya/AGENTS.md` (repo-level) — a Codex-adapted version of `CLAUDE.md`'s session-start briefing, memory file map, trigger-phrase → `SKILL.md` table, and exit/farewell protocol, since Codex needs the skill instructions spelled out explicitly rather than relying on auto-trigger.
7. Fendy asked whether any of this would break Claude Code's ability to read the repo — confirmed `CLAUDE.md` was untouched (same file, same timestamp) and that Claude Code and Codex read entirely separate instruction files, so both coexist without conflict.

### Key Insights & Learning
- Codex CLI's instruction-loading model (global `~/.codex/AGENTS.md` + repo-level `AGENTS.md`, merged) is structurally similar to Claude Code's `CLAUDE.md` but has no equivalent of auto-triggered skills — anything that depended on `plugins/ses-skills/` auto-detection had to be rewritten as an explicit "read this file when you see this phrase" table.
- The `main/repos.md` registry had been silently wrong (ServBay/www paths that don't exist) — fixing it benefits Claude Code too, since the repo-switcher skill reads the same file.

### Collaboration Highlights
- Fendy caught the path assumption early ("check others repo too, because it should be at /applications/sites/") before I'd only fixed Jiraiya's own entry — led to verifying and correcting all 6 registry rows instead of just one.

### Growth & Development Notes
- Cross-tool setups (Claude Code + Codex sharing one memory core) should always confirm each tool's actual instruction-loading mechanism before assuming feature parity, rather than porting a protocol 1:1.

### Looking Forward
- Fendy still needs to run `codex login` (or provide an API key) — `codex doctor` confirmed the CLI is installed and correctly detecting the jiraiya repo root, but no auth credentials exist yet.
- Standing note: when saving diary/CR entries for the ICAN2U repo specifically, skip the CR log step even though its `.env` category is `UiTM` — Fendy's explicit instruction, not yet reflected in `.env` or the save-diary skill itself.

---
