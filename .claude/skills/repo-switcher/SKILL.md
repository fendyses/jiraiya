---
name: repo-switcher
description: "List the registered repositories under /Applications/Sites and switch the active working repo. Use when the user types '/repo-switcher', '/repo', 'switch repo', 'change repo', 'list repos', or 'add repo [name] [path]'."
---

# /repo-switcher

This is a pointer skill. The authoritative protocol lives in the JIRAIYA memory core.

**Read `/Applications/Sites/jiraiya/plugins/ses-skills/skills/repo-switcher/SKILL.md`
in full and follow every step of it**, treating it as if it were written directly
here. Do not summarise or skip steps.

Any arguments passed after `/repo-switcher` are that skill's input.

Same skill as `/repo`.
