---
name: repo
description: "Alias for /repo-switcher. List the registered repositories under /Applications/Sites and switch the active working repo. Use when the user types '/repo', 'switch repo', 'change repo', or 'list repos'."
---

# /repo

Short alias for `/repo-switcher`. The authoritative protocol lives in the
JIRAIYA memory core.

**Read `/Applications/Sites/jiraiya/plugins/ses-skills/skills/repo-switcher/SKILL.md`
in full and follow every step of it**, treating it as if it were written directly
here. Do not summarise or skip steps.

Any arguments passed after `/repo` are that skill's input.
