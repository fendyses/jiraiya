---
name: jiraiya-recall
description: "Perform on-demand deep workspace recall by loading the matching project context, current-session memory, and relevant reminders, then present a concise continuation path. Use when the user says 'JIRAIYA', 'recall', 'ingat semula', requests full workspace context, or starts in a known workspace where Session Briefing is unavailable."
---

# /jiraiya-recall

This is a pointer skill. The authoritative protocol lives in the JIRAIYA memory core.

**Read `plugins/ses-skills/skills/jiraiya-recall/SKILL.md` in full and follow every step of it**, treating it as if it were written directly here. Do not summarise or skip steps.

Any arguments passed after `/jiraiya-recall` are that skill's input.
