---
name: library
description: "MUST use when saving to the library, loading from the library, searching for existing knowledge, installing pre-made items, or when about to create a new library entry. Also triggers when user says 'save library', 'load library', 'check library', 'search library', 'install item', 'install library item', 'do we have', 'is there a pattern for', 'find in library', or when the AI independently decides to save reusable knowledge."
---

# /library

This is a pointer skill. The authoritative protocol lives in the JIRAIYA memory core.

**Read `plugins/ses-skills/skills/library/SKILL.md` in full and follow every step of it**, treating it as if it were written directly here. Do not summarise or skip steps.

Any arguments passed after `/library` are that skill's input.
