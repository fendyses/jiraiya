---
name: session-briefing
description: "Deliver the automatic startup brief by restoring the last-session recap, ongoing To Do items, project health, time context, and queued inbox work in no more than twelve lines. Use at every session start unless the user says 'skip brief', and when the user says 'brief', 'session brief', 'what did we do last time', or 'where did we leave off'."
---

# /session-briefing

This is a pointer skill. The authoritative protocol lives in the JIRAIYA memory core.

**Read `plugins/ses-skills/skills/session-briefing/SKILL.md` in full and follow every step of it**, treating it as if it were written directly here. Do not summarise or skip steps.

Any arguments passed after `/session-briefing` are that skill's input.
